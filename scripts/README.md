# Scripts

This directory is reserved for repository-owned maintenance scripts.

Optional developer tooling can be installed manually when needed:

```sh
cargo install cargo-deny cargo-audit cargo-cyclonedx release-plz cargo-machete
```
